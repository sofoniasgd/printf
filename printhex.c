#include "main.h"
/**
 * printhex - prints a number in hexadcimal
 * @number: pointer to number to be printed
 * @flag: flag charatcter
 * @lengthmod: length modifier
 * Return: number of digits printed
 */
int printhex(__attribute__((unused))int *number, char flag, char lengthmod)
{
if (flag == 1)
	return (1);
if (lengthmod == 1)
	return (0);
return (0);
}

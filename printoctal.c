#include "main.h"
/**
 * printoctal - prints a number on octal
 * @number: number to be printed
 * @flag: flag specifier
 * @lengthmod: length specifier
 * Return: number of digits printed
 */
int printoctal(__attribute__((unused))int *number, char flag, char lengthmod)
{
if (flag == '0')
	return (0);
if (lengthmod == '0')
	return (0);
return (0);
}
